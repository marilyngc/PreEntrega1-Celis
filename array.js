// Escribir un programa en donde el usuario ingrese números,
// esos números se carguen en un array. Luego recorrer el array y determinar
// cuál es el mayor, el menor y la media.

// let numerosIngresados = [];
// let numeros = 0;

// do {
//   let numeros = parseInt(prompt("Ingrese la cantidad de números que desee"));
//   numerosIngresados.push(numeros);
//   console.log(numerosIngresados);
//   numeros++;
// } while (numeros == 0);
